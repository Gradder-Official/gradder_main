from api.classes import Teacher, Student, Parent, Admin

# Converts the string to the Class type
TYPE_DICTIONARY = {
    'Teacher': Teacher,
    'Student': Student,
    'Parent': Parent,
    'Admin': Admin
}